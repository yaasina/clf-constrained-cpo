# Weights & Biases run configuration
# These values are read by _init_wandb_run() in training.py (via wandb.init directly)
project: "clf-constrained-cpo"
name: ${experiment.name}
save_dir: ${hydra:runtime.output_dir}/logs
notes: ${experiment.description}
tags: ["pipeline"]
group: ${experiment.name}
offline: false
